有关地图的类再整理一下

MapDataParse和LevelDataParse两个类能否合并,尤其是MapController和EliminateLogic

是不是还缺少了一个PropElement类,还是说BasicElement可以不要

getter方法,还不如直接访问私有变量

命名规范——顺眼为主、辨识度

对外使用的变量,需要全命名

如果是内部细节,变量命名可以简写

单词组合：形容词+名词、名词+名词

UPDATE_MAP和UPDATE_VIEW_OVER合并

不同的资源放到单独的文件夹下

游戏元素之间的间隔、整个地图左右留空的像素,用全局变量保存这些数据,以免修改起来方便,也说不定在不同的屏幕尺寸下经常需要修改这些参数

锚点设置的问题、原点坐标

每个游戏元素锚点设置为正中心可能会比较好

如果是那种背景图或者固定定位的显示对象,不需要对其设置锚点

如果显示对象经常需要做动画,经常会跑的,那就要考虑锚点的问题了